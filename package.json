{
  "name": "donorlens",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "format": "pnpm -r format",
    "dev:infra": "docker compose up -d postgres keycloak-db keycloak kafka redis",
    "stop:infra": "docker compose down"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
