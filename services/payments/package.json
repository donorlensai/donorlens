{
  "name": "@donorlens/payments-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd ../.. && ./node_modules/.bin/tsc -p services/payments/tsconfig.json --noEmit --diagnostics",
    "typecheck": "cd ../.. && ./node_modules/.bin/tsc -p services/payments/tsconfig.json --noEmit --diagnostics",
    "test": "echo \"No payments service tests configured yet\"",
    "lint": "echo \"No payments service lint command configured yet\"",
    "format": "echo \"No payments service format command configured yet\""
  }
}
