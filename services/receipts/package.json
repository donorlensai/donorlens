{
  "name": "@donorlens/receipts-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd ../.. && ./node_modules/.bin/tsc -p services/receipts/tsconfig.json --noEmit --diagnostics",
    "typecheck": "cd ../.. && ./node_modules/.bin/tsc -p services/receipts/tsconfig.json --noEmit --diagnostics",
    "test": "echo \"No receipts service tests configured yet\"",
    "lint": "echo \"No receipts service lint command configured yet\"",
    "format": "echo \"No receipts service format command configured yet\""
  }
}
