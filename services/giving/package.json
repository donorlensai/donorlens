{
  "name": "@donorlens/giving-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd ../.. && ./node_modules/.bin/tsc -p services/giving/tsconfig.json --noEmit --diagnostics",
    "typecheck": "cd ../.. && ./node_modules/.bin/tsc -p services/giving/tsconfig.json --noEmit --diagnostics",
    "test": "echo \"No giving service tests configured yet\"",
    "lint": "echo \"No giving service lint command configured yet\"",
    "format": "echo \"No giving service format command configured yet\""
  }
}
