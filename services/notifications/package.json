{
  "name": "@donorlens/notifications-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd ../.. && ./node_modules/.bin/tsc -p services/notifications/tsconfig.json --noEmit --diagnostics",
    "typecheck": "cd ../.. && ./node_modules/.bin/tsc -p services/notifications/tsconfig.json --noEmit --diagnostics",
    "test": "echo \"No notifications service tests configured yet\"",
    "lint": "echo \"No notifications service lint command configured yet\"",
    "format": "echo \"No notifications service format command configured yet\""
  }
}
