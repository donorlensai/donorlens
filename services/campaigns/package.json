{
  "name": "@donorlens/campaigns-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd ../.. && ./node_modules/.bin/tsc -p services/campaigns/tsconfig.json --noEmit --diagnostics",
    "typecheck": "cd ../.. && ./node_modules/.bin/tsc -p services/campaigns/tsconfig.json --noEmit --diagnostics",
    "test": "echo \"No campaigns service tests configured yet\"",
    "lint": "echo \"No campaigns service lint command configured yet\"",
    "format": "echo \"No campaigns service format command configured yet\""
  }
}
