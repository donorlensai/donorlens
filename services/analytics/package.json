{
  "name": "@donorlens/analytics-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd ../.. && ./node_modules/.bin/tsc -p services/analytics/tsconfig.json --noEmit --diagnostics",
    "typecheck": "cd ../.. && ./node_modules/.bin/tsc -p services/analytics/tsconfig.json --noEmit --diagnostics",
    "test": "echo \"No analytics service tests configured yet\"",
    "lint": "echo \"No analytics service lint command configured yet\"",
    "format": "echo \"No analytics service format command configured yet\""
  }
}
