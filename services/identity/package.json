{
  "name": "@donorlens/identity-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd ../.. && ./node_modules/.bin/tsc -p services/identity/tsconfig.json --noEmit --diagnostics",
    "typecheck": "cd ../.. && ./node_modules/.bin/tsc -p services/identity/tsconfig.json --noEmit --diagnostics",
    "test": "node --test tests/*.test.ts",
    "lint": "echo \"No identity service lint command configured yet\"",
    "format": "echo \"No identity service format command configured yet\""
  }
}
