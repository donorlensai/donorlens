{
  "name": "@donorlens/files-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"No files service tests configured yet\"",
    "lint": "echo \"No files service lint command configured yet\"",
    "format": "echo \"No files service format command configured yet\""
  }
}
