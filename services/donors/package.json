{
  "name": "@donorlens/donors-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd ../.. && ./node_modules/.bin/tsc -p services/donors/tsconfig.json --noEmit --diagnostics",
    "typecheck": "cd ../.. && ./node_modules/.bin/tsc -p services/donors/tsconfig.json --noEmit --diagnostics",
    "test": "echo \"No donors service tests configured yet\"",
    "lint": "echo \"No donors service lint command configured yet\"",
    "format": "echo \"No donors service format command configured yet\""
  }
}
