{
  "name": "@donorlens/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "cd ../.. && ./node_modules/.bin/tsc -p packages/contracts/tsconfig.json --noEmit --diagnostics",
    "typecheck": "cd ../.. && ./node_modules/.bin/tsc -p packages/contracts/tsconfig.json --noEmit --diagnostics",
    "test": "echo \"No contract tests configured yet\"",
    "lint": "echo \"No lint command configured yet\"",
    "format": "echo \"No format command configured yet\""
  }
}
