# Config Package

Shared configuration primitives belong here.

## Rules

- Do not store secrets in this package.
- Keep environment-specific values outside source control.
- Validate configuration at service startup.
